services = "$(HOME)/services"
myprogram {
        name = "myprogram"
        root = "$(services)/$(name)"
        exec = "$(root)/$(name)"
        stdout = "$(root)/stdout"
        stderr = "$(root)/stderr"
        delay = 1
}
